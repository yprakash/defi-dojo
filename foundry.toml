[profile.default]
src = "."  # Tells Foundry to look in the current directory (and subdirectories) for Solidity files.
out = "out"  # Specifies where the compiled artifacts will be saved
libs = ["lib"]
solc_version = "0.8.29"

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
